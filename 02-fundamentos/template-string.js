const name = 'Negative'
const real = 'Mike'
const normal = name + ' ' + real
const template = `${name} ${real}`
console.log(normal)
console.log(template)
console.log(normal === template)

const html = `
<h1>Hola</h1>
<p>Mundo</p>
`
console.log(html)